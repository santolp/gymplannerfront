import React, {useEffect, useState} from 'react'; import api from '../api'; export default function WorkoutDay(){ const [data,setData]=useState(null); const userId = localStorage.getItem('userId') || ''; const load=async ()=>{ try{ const r=await api.get(`/api/workout/day`, { params: { userId, date: new Date().toISOString() } }); setData(r.data); }catch(e){ console.error(e); alert('No plan or API'); } }; useEffect(()=>{ load();},[]); return (<div style={{padding:20}}><h2>Workout Day (demo)</h2><pre>{JSON.stringify(data, null,2)}</pre></div>)}